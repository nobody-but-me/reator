# Reator - a React calculator

A simple -- but useful -- calculator written in JSX, HTML and CSS, using React frontend library and Vite frontend tool.

# Running

Run the source code:

- npm run dev

